Steps to build and install package:

1. Install needed packages
    sudo apt-get update
    sudo apt-get install -y debhelper golang-1.13 git devscripts

2. Install docker https://docs.docker.com/engine/install/ubuntu/

3. Install amazon-efs-utils https://docs.aws.amazon.com/efs/latest/ug/installing-other-distro.html

4. Build the package by running
    sudo DEB_SIGN=0 make clean ubuntu-focal

5. Install the package with
    sudo dpkg -i pathToDEBFile
